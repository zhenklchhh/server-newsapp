/**
 * Package containing classes for
 * JSON Web Token (JWT) generation and validation.
 *
 * <p>
 * This package provides utilities
 * for handling JWTs, including creation (signing),
 * and validation. Key components include:
 *   <ul>
 *     <li>{@link com.logic.server_newsapp.jwt.JWTGenerator}:
 *         A class responsible for generating JWTs,
 *         including setting headers, claims, and signing.</li>
 *     <li>
 *       <em>Other JWT related utility classes if any</em>:
 *       If you have other utility classes related to JWTs here, document them.
 *      </li>
 *   </ul>
 * </p>
 *
 * <p>
 *   This package leverages a library for
 *   JWT operations, such as Nimbus JOSE + JWT.
 * </p>
 */
package com.logic.server_newsapp.jwt;
